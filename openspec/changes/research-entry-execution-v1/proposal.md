# Proposal: Research entry execution v1

Implement the first production slice of the new authoritative simulator: consume aligned Strategy Engine entry decisions and MDS candles, execute signal-bar-close entries, and create immutable open position state without importing legacy runtime code.
