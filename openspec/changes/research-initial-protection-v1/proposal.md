# Proposal: Research initial protection v1

Consume Strategy Engine static exit-policy ratios and readiness on the entry bar, preserve the legacy `entries & stop_ready` gate, and attach absolute initial stop/take levels to the new Research Service position state without importing legacy runtime code.
