# Requirements
- `/api/research/config/validate` SHALL preserve the Workbench response DTO.
- Research Service SHALL validate only research/execution envelope semantics locally.
- Strategy instance semantics SHALL be delegated to Strategy Engine.
- Upstream validation errors SHALL retain stable paths and messages.
