# Proposal: Research single-instance backtest v1

Compose the already ported strategy-consumer, market, execution and accounting layers into one authoritative use case for one strategy instance. Do not activate the public backtest endpoint or persist artifacts in this change.
