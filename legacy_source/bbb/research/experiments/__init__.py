"""Batch Experiment Management System (capability: research-experiments).

Experiment BatchRunner v1 (``batch_runner.py``) is the first execution module: it runs
validated single-instance candidate configs sequentially via the ema_pullback strategy runner.
"""
